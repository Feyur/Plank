{
  "name": "@plank/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Десктоп-обёртка Plank под macOS (Tauri)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build --bundles app dmg && node scripts/collect-release.mjs",
    "build:app": "tauri build --bundles app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
